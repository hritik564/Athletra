Act as a Senior Biomechanics Engineer. I am building a sports analysis app (Athletra). I need a robust, real-time signal processing pipeline that converts raw MediaPipe pose landmarks into stable biomechanical signals. Write a Python module with a class: AthletraSignalProcessor This class will be used in a real-time loop: processor.update(landmarks, timestamp) ---------------------------------- INPUT FORMAT ---------------------------------- landmarks is a list of 33 elements (MediaPipe format), where each landmark is: { "x": float, "y": float, "z": float, "visibility": float } ---------------------------------- CORE REQUIREMENTS ---------------------------------- Implement 4 stages: ---------------------------------- 1. NORMALIZATION (Distance Independence) ---------------------------------- - Compute shoulder_width = distance between left_shoulder (11) and right_shoulder (12) - Use this as scale reference (1.0) - All distances (velocity, displacement) must be normalized by this value ---------------------------------- 2. SMOOTHING (Jitter Reduction) ---------------------------------- - Implement One-Euro Filter OR exponential low-pass filter - Apply per landmark (x, y, z) - Ignore landmarks with visibility < 0.5 - Handle missing frames (None or low confidence) - Maintain internal state for previous frames ---------------------------------- 3. HEURISTIC GATING (Event Detection) ---------------------------------- - Compute velocity of a target landmark (e.g., wrist index 15) - Velocity = distance(frame_t - frame_t-1) / delta_time - Normalize velocity using shoulder_width Gate Logic: - Gate OPEN when velocity > threshold_open - Gate CLOSE when velocity < threshold_close - Maintain a buffer of frames while gate is open - Return captured segment when gate closes ---------------------------------- 4. UPSAMPLING (Peak Detection) ---------------------------------- - Use scipy.interpolate.CubicSpline - Input: buffered angle or coordinate sequence - Upsample by 10x - Return: - interpolated_peak_value - original_peak_value - confidence flag IMPORTANT: - If frames < 4 → skip interpolation - Never hallucinate peak if insufficient data ---------------------------------- API DESIGN ---------------------------------- processor.update(landmarks, timestamp) Returns: { "gate_open": bool, "segment_complete": bool, "peak_value": float or None, "raw_velocity": float, } ---------------------------------- CONSTRAINTS ---------------------------------- - Must run efficiently in Replit (low memory) - No heavy object creation per frame - Use numpy for vector math - Use scipy only for interpolation - Keep code modular and clean ---------------------------------- BONUS (Optional) ---------------------------------- - Add debug mode to visualize: - velocity curve - gate state - Implement temporal hysteresis in gating (minimum open/close duration) - Validate shoulder_width stability before normalization; fallback if unstable - Use timestamps (not frame index) for interpolation - Define peak metric explicitly (angle or velocity) - Return peak metadata (time, duration, confidence)